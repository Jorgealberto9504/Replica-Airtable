{
  "name": "airtable-clone",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev:backend": "npm run -w apps/backend dev",
    "dev": "npm run dev:backend"
  }
}